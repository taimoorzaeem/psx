import React from 'react';
import Classes from './DataSlider.css';

const dataSlider = (props) => {
    return (
        <div id="DataSlider" className={Classes.body}>Data Slider</div>
    );
}

export default dataSlider;